services:
  poc:
    build: .
    image: poc:latest
    command: ["-c", "prod-env.conf", "-i", "factory"]
    restart: always
    ports:
      - "443:80"
    volumes:
      - odoo-poc-fs:/var/lib/odoo

volumes:
  odoo-poc-fs:
